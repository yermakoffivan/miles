"""SGLang-derived Anthropic Messages wire codec for the session server.

``protocol.py`` is a frozen verbatim snapshot of the upstream Pydantic models;
``codec.py`` is the pure conversion layer extracted from the upstream serving
handler. Provenance, checksums, and the adaptation ledger live in UPSTREAM.md.
"""
